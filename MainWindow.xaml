<Window x:Class="TaskbarTextOverlayWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="Auto"
        TextOptions.TextHintingMode="Fixed">
    <Grid IsHitTestVisible="False">
        <Border Padding="12" CornerRadius="8" Background="#00FFFFFF">
            <Grid ClipToBounds="True">
                <TextBlock x:Name="MonoText"
                           Text="{Binding Text}"
                           Foreground="{Binding ForegroundBrush}"
                           FontFamily="{Binding FontFamily}"
                           FontSize="{Binding FontSize}"
                           FontWeight="{Binding FontWeightValue}"
                           TextAlignment="{Binding Alignment}"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis">
                    <TextBlock.Effect>
                        <DropShadowEffect ShadowDepth="0"
                                          BlurRadius="{Binding ShadowBlur}"
                                          Opacity="{Binding ShadowOpacity}"/>
                    </TextBlock.Effect>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UseRich}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <ItemsControl x:Name="RichLine"
                              ItemsSource="{Binding RichSegments}"
                              VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UseRich}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Alignment}" Value="Left">
                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Alignment}" Value="Right">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Alignment}" Value="Center">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Alignment}" Value="Justify">
                                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}"
                                       Foreground="{Binding ForegroundBrush}"
                                       FontFamily="{Binding DataContext.FontFamily, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       FontWeight="{Binding DataContext.FontWeightValue, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       TextTrimming="None">
                                <TextBlock.Effect>
                                    <DropShadowEffect ShadowDepth="0"
                                                      BlurRadius="{Binding DataContext.ShadowBlur, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      Opacity="{Binding DataContext.ShadowOpacity, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</Window>
